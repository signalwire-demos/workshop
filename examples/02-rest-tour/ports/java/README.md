# Pillar 2 — REST Tour (Java)

## Run

```bash
gradle run
# Open http://localhost:8001
```

Javalin + `RestClient.builder().project().token().space().build()`. Same 4 demo endpoints as Python/TS/Ruby/Go.
