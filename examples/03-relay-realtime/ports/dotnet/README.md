# Pillar 3 — RELAY Realtime (.NET / C#)

```bash
dotnet run
# Open http://localhost:8002
```

ASP.NET Core minimal API + `app.UseWebSockets()` + `SignalWire.Relay.Client`. OnCall handler broadcasts events to all attached browser WebSockets.
